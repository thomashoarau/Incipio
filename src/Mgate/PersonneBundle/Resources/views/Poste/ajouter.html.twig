{% extends "MgatePersonneBundle::layout.html.twig" %}

{% block title %}
    {{ 'position.add_position'|trans({}, 'person') }} {{ parent() }}
{% endblock %}

{% block content_title %}
    {{ 'position.add_position'|trans({}, 'person') }} {{ param('abbr') }}
{% endblock %}

{% block breadcrumb %}
    <ul class="breadcrumb no-margin pull-right">
        <li><a href="{{ path('MgatePersonne_poste_homepage') }}">{{ 'dashboard.postes_filieres'|trans({}, 'dashboard') }}</a></li>
        <li class="active">{{ 'position.add_position'|trans({}, 'person') }}</li>
    </ul>
{% endblock %}

{% block content_bundle %}
    <div class="table-responsive content">
        {{ form_start(form) }}
        <table class="table table-bordered table-striped">
            <tr>
                <th>{{ form_label(form.intitule) }}</th>
                <td>{{ form_widget(form.intitule) }} {{ form_errors(form.intitule) }}</td>
            </tr>
            <tr>
                <th>{{ form_label(form.description) }}</th>
                <td>{{ form_widget(form.description) }} {{ form_errors(form.description) }}</td>
            </tr>
            <tr>
                <td colspan="2">
                    <div class="row">
                        <div class="col-md-2 col-xs-4">
                            <button type="submit" class="btn btn-success">{{ 'generic.save'|trans({}, 'person') }}</button>
                            {{ form_rest(form) }}
                            {{ form_end(form) }}
                        </div>
                    </div>
                </td>
            </tr>
        </table>
    </div>
{% endblock %}

