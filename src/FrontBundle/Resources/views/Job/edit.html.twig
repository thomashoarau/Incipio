{% extends 'FrontBundle:CRUD:show.html.twig' %}

{%- set jobId = job['@id']|uriId -%}

{% form_theme edit_form 'bootstrap_3_horizontal_layout.html.twig' %}

{% block page_header_title -%}
    <h1>Poste #{{ jobId }}</h1>
{%- endblock %}

{% block page_header_button -%}
    {{ macros.button_entity_list(path('jobs'), 'Retour à la liste') }}
{%- endblock %}

{% block sub_body -%}

    <div class="container">
        <div class="col-md-6">
            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                {{ macros.button_link(path('jobs_show', { 'id': jobId }), 'Annuler', 'btn-default pull-left') }}
                <input type="submit" value="Enregistrer" class="btn btn-primary pull-right" />
            {{ form_end(edit_form) }}
        </div>
    </div>
{%- endblock %}
